#!/bin/bash -e

TAG="openthread-border-router.remove"

logger --tag=$TAG "Start"

logger --tag=$TAG "Un-configure the firewall"
$SNAP/bin/script/otbr-firewall stop

logger --tag=$TAG "Remove IP forwarding"
source $SNAP/bin/script/_ipforward
ipforward_uninstall

logger --tag=$TAG "End"
