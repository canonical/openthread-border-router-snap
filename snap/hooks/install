#!/bin/bash -e

TAG="openthread-border-router.install"

logger --tag=$TAG "Start"

logger --tag=$TAG "Configure the firewall"
$SNAP/bin/script/otbr-firewall start

logger --tag=$TAG "Setup IP forwarding"
source $SNAP/bin/script/_ipforward
ipforward_install

logger --tag=$TAG "End"
